{
    "name": "importer",
    "version": "1.0.0",
    "description": "Example Lambda for SQS Worker to process json files in from S3 and import them into a DynamoDb database",
    "main": "handler.js",
    "scripts": {
      "deploy": "serverless deploy --stage staging",
      "dev": "serverless offline start --httpPort 8080 --region localhost",
      "test": "NODE_ENV=test jest"
    },
    "dependencies": {
        "aws-sdk": "^2.1054.0",
        "serverless-http": "^2.7.0"
    },
    "devDependencies": {
        "serverless-lift": "^1.1.2",
        "serverless-offline": "^12.0.4"
    }
  }
  