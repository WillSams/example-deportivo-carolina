extends layouts/default

block css
  link(href='https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.7.0/animate.min.css' rel='stylesheet')
  
mixin announcements(announcements)
  .announcements-item
    .col-md-3
      h3.text-weight-light.black-text {announcements.title}
      h4 {announcements.subtitle} 
      p.white-text 
        a(href='') Read
    .col-md-3
      | Image Here
      //img.center-block.img-responsive(src=announcement.image.url height='380').img-rounded 

mixin sponsor(sponsor)
  .sponsor-item
    | Sponsor Image
    //img(src=galleryImage.image.url, alt='' width='338' height='244') 

block content
  #main-content 
    .row.mx-auto
      .col-md-4
        #schedules-area(style='margin: 15px;  height: 260px;')
          h2 Schedules
        #fields-area(style='margin: 15px;  height: 130px;') 
          h2 Fields 
      .col-md-8
        #announcements-area(style='margin: 15px; height: 425px;')
          h2 Announcements
          .announcements-carousel.owl-carousel.white-background.rounded
            //each announcement in data.announcements
            //  +announcement(announcement)
    .row.mx-auto
      .col-md-12.center-block
        #sponsors-area(style='margin: 15px; height: 130px;')   
          h2 Sponsors
            .sponsors-carousel.owl-carousel.white-background.rounded
          //each sponsor in data.sponsors
          //  +sponsor(sponsor) 
    .row.mx-auto
      .col-md-12.center-block
        #calendar-area(style='margin: 15px; height: 380px;')
          h2 Calendar

block js
  script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.4.1/jquery.easing.min.js')
  script.
    $(document).ready(function () {      
      $('.announcements-carousel').owlCarousel({
        autoplay: true, dots: true, loop: true, pagination: true, navigation: true,
        responsive: { 0: { items: 1 }, 768: { items: 1 }, 900: { items: 1 } }
      });
      
      $('.sponsors-carousel').owlCarousel({
        autoplay: true, loop: true, pagination: false,
        responsive: { 0: { items: 2 }, 768: { items: 4 }, 900: { items: 8 } }
      });

      $('.back-to-top').click(function () {
        $('html, body').animate({ scrollTop: 0 }, 1500, 'easeInOutExpo');
        return false;
      });  
    })